---
title: "LB Checklists"
date: 2025-09-03
tags: [lb, network, checklist]
categories: [Howtos, Network]
---

## SCP LB의 사용자 접근 및 설정 가능성
SCP LB는 Application Load Balancer(ALB)와 Network Load Balancer(NLB) 유형을 지원하며, SKE(SCP Kubernetes Engine) 환경에서 Service type=LoadBalancer를 통해 연동됨. 사용자는 다음과 같은 방식으로 접근:
- **콘솔 접근**: SCP 포털(https://cloud.samsungsds.com/)에서 'All Services > Networking > Load Balancer' 메뉴를 통해 LB 서버 그룹을 선택하고 생성/수정함. 예를 들어, LB 생성 시 포트 매핑, 헬스 체크 경로, 백엔드 인스턴스 등록 등을 설정 가능함.
- **CLI 접근**: SCP CLI 도구(https://github.com/t2yijaeho/SCP-CLI)를 통해 명령어로 LB 관리 가능함. 이는 자동화 스크립트나 CI/CD 통합에 유용함.
- **API 접근**: SCP API를 통해 프로그래밍 방식으로 LB 구성 변경 가능함. 이는 IaC(Infrastructure as Code) 도구처럼 Terraform이나 Ansible과 연동됨.
- **Kubernetes 연동(SKE)**: SKE에서 kubectl 명령으로 Service YAML을 수정하여 LB 어노테이션(예: sessionAffinity, externalTrafficPolicy)을 적용함. 이는 사용자 수준에서 직접 변경 가능함.

가능한 주요 설정 항목(사용자 변경 가능):
- LB 유형 선택(ALB/NLB), 포트 매핑(port/targetPort), 헬스 체크(경로, 주기, 임계값), 세션 어피니티, 외부 트래픽 정책, 보안 그룹 규칙, SSL/TLS 인증서 적용, 타임아웃 값, 로그 활성화, DNS 레코드 연동, IP 화이트리스트, 스케일링 정책 연동(HPA), 리소스 태그 등.
- 제한 사항: 플랫폼 수준의 LB 인프라(예: 물리적 서버 배치, 글로벌 라우팅)는 매니지드되어 사용자 직접 변경 불가함. 이는 안정성과 보안을 위한 설계임. 만약 초과 사용 시 쿼터 제한이 적용될 수 있음.

## 구성

| 점검 내용 | 점검 방법 | 점검 기준 | 취약 시 문제점 | 개선 방안 | 참고 |
|-----------|-----------|-----------|---------------|-----------|-------|
| **로드밸런서 유형 설정** | SCP 콘솔에서 LB 설정 확인 또는 `kubectl describe svc <service-name>` 실행 | 정상: Application LB 또는 Network LB가 워크로드에 맞게 설정됨<br>취약: 워크로드 요구사항과 맞지 않는 LB 유형 사용 | 부적절한 LB 유형 사용 시 성능 저하 또는 트래픽 처리 실패 발생 가능 | 워크로드 특성에 따라 ALB 또는 NLB 선택, HTTP 기반은 ALB, TCP/UDP는 NLB 권장 | * 설정방법: SCP 콘솔에서 LB 유형 변경 또는 `service.spec.type=LoadBalancer` 설정<br>* References: [Samsung Cloud Platform LB Overview](https://www.samsungsds.com/kr/cloud/service/scp_loadbalancer.html) |
| **헬스 체크 설정** | SCP 콘솔에서 헬스 체크 경로 및 포트 확인 또는 `kubectl describe svc`로 어노테이션 확인 | 정상: 헬스 체크 경로 및 포트가 백엔드 애플리케이션 상태를 정확히 반영<br>취약: 헬스 체크 경로 또는 포트 미설정 또는 부정확 | 잘못된 헬스 체크로 인해 정상 백엔드가 비정상으로 판단되거나, 비정상 백엔드가 트래픽 수신 가능 | 헬스 체크 경로를 애플리케이션의 상태 엔드포인트(예: `/health`)로 설정, 포트 일치 확인 | * 확인방법: `kubectl get svc -o yaml`로 `healthCheckNodePort` 확인<br>* References: [Kubernetes Service Health Check](https://kubernetes.io/docs/tasks/access-application-cluster/create-external-load-balancer/) |
| **포트 매핑 설정** | SCP 콘솔 또는 `kubectl get svc -o yaml`로 포트 설정 확인 | 정상: LB 포트와 백엔드 포트가 애플리케이션 요구사항에 맞게 매핑됨<br>취약: 포트 불일치 또는 불필요한 포트 오픈 | 포트 불일치 시 트래픽 전달 실패, 불필요한 포트 오픈 시 보안 위험 증가 | LB 포트와 백엔드 포트를 일치시키고, 최소한의 포트만 오픈 | * 설정방법: `service.spec.ports`에 `port`와 `targetPort` 명시<br>* References: [Kubernetes Service Ports](https://kubernetes.io/docs/concepts/services-networking/service/#defining-a-service) |
| **세션 어피니티 설정** | SCP 콘솔에서 세션 유지 설정 확인 또는 `kubectl get svc -o yaml`로 `sessionAffinity` 확인 | 정상: `sessionAffinity=ClientIP` 또는 애플리케이션 요구사항에 맞는 설정<br>취약: 세션 어피니티 미설정으로 세션 불일치 발생 | 세션 불일치로 인해 사용자 경험이 저하되거나 애플리케이션 오류 발생 | 애플리케이션 요구사항에 따라 `sessionAffinity=ClientIP` 설정 또는 쿠키 기반 세션 유지 활성화 | * 설정방법: `service.spec.sessionAffinity=ClientIP`<br>* References: [Kubernetes Session Affinity](https://kubernetes.io/docs/concepts/services-networking/service/#service-session-affinity) |
| **외부 트래픽 정책** | `kubectl get svc -o yaml`로 `externalTrafficPolicy` 확인 | 정상: `externalTrafficPolicy=Local` 또는 `Cluster`가 요구사항에 맞게 설정<br>취약: 부적절한 트래픽 정책으로 성능 저하 | `Cluster` 모드에서 불필요한 노드 간 트래픽 발생, `Local` 모드에서 헬스 체크 실패 시 트래픽 손실 | 워크로드 특성에 따라 `Local` 또는 `Cluster` 선택, `Local` 사용 시 헬스 체크 강화 | * 설정방법: `service.spec.externalTrafficPolicy=Local`<br>* References: [Kubernetes External Traffic Policy](https://kubernetes.io/docs/tasks/access-application-cluster/create-external-load-balancer/#preserving-the-client-source-ip) |
| **로드밸런서 보안 그룹 설정** | SCP 콘솔에서 LB와 연결된 보안 그룹 확인 | 정상: LB 보안 그룹이 허용된 IP/포트만 오픈<br>취약: 모든 IP(0.0.0.0/0)로 포트 오픈 | 불필요한 포트 오픈으로 보안 취약점 노출 | 최소 권한 원칙에 따라 보안 그룹 설정, 특정 CIDR 및 포트만 허용 | * 설정방법: SCP 콘솔에서 보안 그룹 규칙 수정<br>* References: [SCP Network Security](https://www.samsungsds.com/kr/cloud/security.html) |
| **SSL/TLS 인증서 설정** | SCP 콘솔에서 LB의 SSL 인증서 확인 | 정상: 유효한 SSL/TLS 인증서가 LB에 적용됨<br>취약: 인증서 만료 또는 미적용 | 인증서 문제로 HTTPS 연결 실패 또는 보안 취 # | 유효한 SSL 인증서를 적용하고, 만료일 모니터링 설정 | * 설정방법: SCP 콘솔에서 인증서 업로드 및 연결<br>* References: [SCP SSL Configuration](https://www.samsungsds.com/kr/cloud/service/scp_loadbalancer.html) |
| **프로토콜 설정** | SCP 콘솔 또는 `kubectl get svc -o yaml`로 프로토콜 확인 | 정상: HTTP/HTTPS 또는 TCP/UDP가 애플리케이션에 맞게 설정<br>취약: 잘못된 프로토콜 사용 | 잘못된 프로토콜로 인해 트래픽 처리 실패 | 애플리케이션 요구사항에 따라 적절한 프로토콜 선택 | * 설정방법: `service.spec.ports.protocol=TCP`<br>* References: [Kubernetes Service Protocols](https://kubernetes.io/docs/concepts/services-networking/service/#protocol-support) |
| **백엔드 인스턴스 등록** | SCP 콘솔 또는 `kubectl describe svc`로 백엔드 상태 확인 | 정상: 모든 백엔드 인스턴스가 정상 등록됨<br>취약: 등록되지 않은 인스턴스 존재 | 등록되지 않은 인스턴스로 인해 트래픽 분배 실패 | `selector` 설정 확인 및 일치하는 파드 레이블 설정 | * 확인방법: `kubectl get endpoints`로 백엔드 확인<br>* References: [Kubernetes Endpoints](https://kubernetes.io/docs/concepts/services-networking/service/#services-without-selectors) |
| **헬스 체크 주기 및 임계값** | SCP 콘솔에서 헬스 체크 주기 및 임계값 확인 | 정상: 주기(예: 10초)와 임계값(예: 3회)이 적절히 설정<br>취약: 지나치게 짧거나 긴 주기 설정 | 부정확한 헬스 체크로 정상 인스턴스 제외 또는 비정상 인스턴스 포함 | 주기를 5~10초, 임계값을 2~3회로 설정 | * 설정방법: SCP 콘솔에서 헬스 체크 설정 수정<br>* References: [Azure Load Balancer Health Checks](https://learn.microsoft.com/ko-kr/azure/load-balancer/load-balancer-custom-probe-overview) |[](https://learn.microsoft.com/en-us/azure/well-architected/service-guides/azure-load-balancer)
| **로드밸런서 리전 설정** | SCP 콘솔에서 LB 리전 확인 | 정상: LB가 애플리케이션 리전과 동일<br>취약: 다른 리전 설정 | 리전 불일치로 인해 지연 시간 증가 | LB를 애플리케이션과 동일 리전에 배포 | * 설정방법: SCP 콘솔에서 LB 리전 설정<br>* References: [SCP Region Configuration](https://www.samsungsds.com/kr/cloud/service/scp_loadbalancer.html) |
| **트래픽 분배 알고리즘** | SCP 콘솔 또는 어노테이션으로 알고리즘 확인 | 정상: Round Robin 또는 Least Connection 설정<br>취약: 부적절한 알고리즘 사용 | 트래픽 불균형으로 특정 인스턴스 과부하 | 워크로드 특성에 맞는 알고리즘 선택 | * 확인방법: SCP 콘솔에서 LB 설정 확인<br>* References: [Load Balancing Algorithms](https://www.cloudflare.com/learning/performance/types-of-load-balancing-algorithms/) |[](https://www.cloudflare.com/learning/performance/types-of-load-balancing-algorithms/)
| **CORS 설정** | SCP 콘솔에서 CORS 정책 확인 | 정상: 필요한 도메인만 CORS 허용<br>취약: 모든 도메인(*) 허용 | 보안 취약점으로 인해 악의적인 요청 허용 가능 | 특정 도메인만 허용하도록 CORS 설정 | * 설정방법: SCP 콘솔에서 CORS 정책 수정<br>* References: [CORS Configuration](https://developer.mozilla.org/ko/docs/Web/HTTP/CORS) |
| **WAF(Web Application Firewall) 통합** | SCP 콘솔에서 WAF 설정 확인 | 정상: WAF가 활성화되고 규칙 설정됨<br>취약: WAF 미적용 | 웹 애플리케이션 공격(예: SQL 인젝션)에 취약 | WAF 활성화 및 OWASP Top 10 규칙 적용 | * 설정방법: SCP 콘솔에서 WAF 설정<br>* References: [SCP WAF](https://www.samsungsds.com/kr/cloud/security.html) |
| **타임아웃 설정** | SCP 콘솔에서 연결 및 유휴 타임아웃 확인 | 정상: 타임아웃 값이 애플리케이션 요구사항에 맞게 설정<br>취약: 지나치게 짧거나 긴 타임아웃 | 연결 끊김 또는 리소스 낭비 발생 | 연결 타임아웃 60초, 유휴 타임아웃 300초로 설정 | * 설정방법: SCP 콘솔에서 타임아웃 설정<br>* References: [AWS ALB Timeout](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/application-load-balancers.html#connection-idle-timeout) |
| **로그 활성화** | SCP 콘솔에서 LB 액세스 로그 설정 확인 | 정상: 액세스 로그가 활성화됨<br>취약: 로그 비활성화 | 문제 진단 및 보안 감사 불가 | LB 액세스 로그 활성화 및 로그 저장소 설정 | * 설정방법: SCP 콘솔에서 로그 설정<br>* References: [AWS ELB Logging](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-access-logs.html) |
| **DNS 설정** | SCP 콘솔 또는 DNS 관리 툴에서 CNAME/A 레코드 확인 | 정상: LB의 DNS 레코드가 정확히 설정됨<br>취약: DNS 레코드 불일치 | 트래픽이 LB로 전달되지 않음 | CNAME 또는 A 레코드를 LB의 공인 IP 또는 도메인에 맞게 설정 | * 확인방법: `nslookup <lb-domain>`<br>* References: [Route 53 Configuration](https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/dns-configuring.html) |
| **IP 화이트리스트** | SCP 콘솔에서 LB 접근 허용 IP 확인 | 정상: 특정 IP 또는 CIDR만 허용<br>취약: 모든 IP(0.0.0.0/0) 허용 | 보안 취약점으로 무단 접근 가능 | 특정 IP/CIDR만 허용하도록 설정 | * 설정방법: SCP 콘솔에서 보안 그룹 수정<br>* References: [SCP Network Security](https://www.samsungsds.com/kr/cloud/security.html) |
| **헬스 체크 프로토콜** | SCP 콘솔에서 헬스 체크 프로토콜 확인 | 정상: HTTP/TCP가 백엔드와 일치<br>취약: 프로토콜 불일치 | 헬스 체크 실패로 정상 백엔드 제외 | 백엔드 애플리케이션 프로토콜과 일치하도록 설정 | * 설정방법: SCP 콘솔에서 헬스 체크 프로토콜 설정<br>* References: [Azure LB Health Probes](https://learn.microsoft.com/ko-kr/azure/load-balancer/load-balancer-custom-probe-overview) |[](https://learn.microsoft.com/en-us/azure/well-architected/service-guides/azure-load-balancer)
| **스케일링 정책** | SCP 콘솔 또는 `kubectl get hpa`로 스케일링 설정 확인 | 정상: 오토스케일링 정책이 LB와 연동됨<br>취약: 스케일링 정책 미설정 | 트래픽 급증 시 성능 저하 | Horizontal Pod Autoscaler(HPA) 설정 및 LB 연동 | * 설정방법: `kubectl autoscale deployment <name> --min=2 --max=10`<br>* References: [Kubernetes HPA](https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/) |
| **리소스 태그 설정** | SCP 콘솔에서 LB 리소스 태그 확인 | 정상: 환경, 소유자 등 태그가 적용됨<br>취약: 태그 미적용 | 리소스 관리 및 비용 추적 어려움 | 환경, 팀, 애플리케이션 이름 등 태그 적용 | * 설정방법: SCP 콘솔에서 태그 추가<br>* References: [AWS Tagging Best Practices](https://docs.aws.amazon.com/organizations/latest/userguide/orgs_tagging.html) |[](https://docs.aws.amazon.com/organizations/latest/userguide/orgs_manage_policies_scps_examples_tagging.html)
| **백엔드 풀 크기** | SCP 콘솔 또는 `kubectl get pods`로 백엔드 확인 | 정상: 최소 2개 이상의 백엔드 인스턴스<br>취약: 단일 인스턴스 | 단일 장애점으로 서비스 중단 위험 | 최소 2개 이상의 파드 배포 | * 확인방법: `kubectl get pods -l app=<app-name>`<br>* References: [Kubernetes High Availability](https://kubernetes.io/docs/setup/production-environment/) |
| **헬스 체크 응답 코드** | SCP 콘솔에서 헬스 체크 응답 코드 확인 | 정상: 200 또는 애플리케이션에 맞는 코드<br>취약: 부정확한 응답 코드 | 헬스 체크 실패로 정상 백엔드 제외 | 애플리케이션의 정상 응답 코드(예: 200) 설정 | * 설정방법: SCP 콘솔에서 헬스 체크 응답 코드 설정<br>* References: [Azure LB Health Probes](https://learn.microsoft.com/ko-kr/azure/load-balancer/load-balancer-custom-probe-overview) |[](https://learn.microsoft.com/en-us/azure/well-architected/service-guides/azure-load-balancer)
| **로드밸런서 이름 규칙** | SCP 콘솔에서 LB 이름 확인 | 정상: 일관된 명명 규칙 준수<br>취약: 비표준 이름 사용 | 리소스 관리 및 식별 어려움 | 환경-애플리케이션-역할 형식의 명명 규칙 적용 | * 설정방법: SCP 콘솔에서 LB 이름 수정<br>* References: [AWS Naming Best Practices](https://docs.aws.amazon.com/organizations/latest/userguide/orgs_tagging.html) |
| **모니터링 통합** | SCP 콘솔에서 모니터링 설정 확인 | 정상: SCP 모니터링 또는 외부 모니터링 도구 통합<br>취약: 모니터링 미설정 | LB 상태 및 성능 문제 파악 불가 | SCP 모니터링 또는 Prometheus 통합 설정 | * 설정방법: SCP 콘솔에서 모니터링 활성화<br>* References: [Prometheus Monitoring](https://prometheus.io/docs/introduction/overview/) |

## 결함 및 오류

| 점검 내용 | 점검 방법 | 점검 기준 | 취약 시 문제점 | 개선 방안 | 참고 |
|-----------|-----------|-----------|---------------|-----------|-------|
| **LB 로그 내 연결 오류 확인** | SCP 콘솔에서 LB 액세스 로그 확인 또는 `kubectl logs -n <namespace> <lb-pod>` 실행 | 정상: 연결 오류 관련 로그(예: connection refused) 없음<br>취약: 연결 오류 로그 존재 | 연결 오류가 발생하면 클라이언트 요청이 백엔드로 전달되지 않아 서비스 중단 발생함, 트래픽 손실로 사용자 경험 저하됨 | LB 로그를 정기적으로 모니터링하고, 연결 오류 원인(예: 백엔드 포트 불일치)을 분석하여 수정함, 자동 알림 설정으로 즉시 대응함 | * 확인방법: SCP 콘솔 로그 뷰어 사용 또는 `kubectl logs`로 오류 키워드 검색<br>* References: [AWS ELB Access Logs](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-access-logs.html) - AWS ELB 액세스 로그 설명 |
| **헬스 체크 실패 로그 확인** | SCP 콘솔 헬스 체크 로그 확인 또는 `kubectl describe svc <service-name>`로 엔드포인트 상태 확인 | 정상: 헬스 체크 실패 로그 없음<br>취약: 헬스 체크 실패(예: probe failed) 로그 존재 | 헬스 체크 실패 시 정상 백엔드가 트래픽에서 제외되어 LB 효율 저하됨, 서비스 가용성 감소함 | 헬스 체크 경로 및 포트를 재설정하고, 백엔드 애플리케이션 상태 확인함, 실패 임계값 조정으로 안정성 높임 | * 확인방법: `kubectl get endpoints`로 실패 엔드포인트 확인<br>* References: [Kubernetes Health Checks](https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/) - Kubernetes 헬스 체크 프로브 설명 |
| **백엔드 연결 실패 오류** | SCP 콘솔에서 LB 백엔드 연결 로그 확인 또는 `kubectl exec -it <pod> -- curl <backend-ip>` 테스트 | 정상: 백엔드 연결 실패 오류 없음<br>취약: 연결 실패(예: connection timed out) 오류 존재 | 백엔드 연결 실패 시 트래픽이 제대로 분배되지 않아 특정 서비스 장애 발생함, 전체 시스템 불안정함 | 네트워크 연결성 테스트하고, 보안 그룹 규칙 수정함, 백엔드 파드 재시작으로 연결 복구함 | * 설정방법: SCP 콘솔에서 백엔드 등록 재확인<br>* References: [Azure Load Balancer Troubleshooting](https://learn.microsoft.com/en-us/azure/load-balancer/load-balancer-troubleshoot) - Azure LB 문제 해결 가이드 |
| **SSL 핸드셰이크 오류 로그** | SCP 콘솔 LB 로그에서 SSL 관련 오류 확인 또는 `openssl s_client -connect <lb-domain>:443` 테스트 | 정상: SSL 핸드셰이크 오류 없음<br>취약: 핸드셰이크 실패(예: handshake failure) 로그 존재 | SSL 오류 시 HTTPS 연결이 성립되지 않아 보안 연결 실패함, 데이터 유출 위험 증가함 | SSL 인증서 재발급 및 적용함, cipher suite 호환성 확인으로 오류 방지함 | * 확인방법: `kubectl logs`로 SSL 키워드 검색<br>* References: [AWS ALB SSL Configuration](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/create-https-listener.html) - AWS ALB SSL 설정 가이드 |
| **타임아웃 오류 확인** | SCP 콘솔 로그에서 타임아웃 오류 확인 또는 `kubectl describe pod <pod-name>`로 이벤트 로그 확인 | 정상: 타임아웃 오류 없음<br>취약: 타임아웃(예: request timeout) 오류 존재 | 타임아웃 발생 시 요청이 중단되어 사용자 대기 시간 증가함, 서비스 응답성 저하됨 | 타임아웃 값을 증가시키고, 백엔드 성능 최적화함, 네트워크 지연 원인 제거함 | * 설정방법: SCP 콘솔에서 타임아웃 설정 수정<br>* References: [Kubernetes Timeout Issues](https://kubernetes.io/docs/tasks/debug/debug-application/debug-service/) - Kubernetes 서비스 디버깅 |
| **DNS 해석 실패 오류** | SCP 콘솔 또는 `kubectl exec -it <pod> -- nslookup <lb-domain>`로 DNS 확인 | 정상: DNS 해석 실패 오류 없음<br>취약: DNS 실패(예: name resolution failed) 오류 존재 | DNS 실패 시 LB 도메인 접근 불가함, 서비스 발견 실패로 전체 연결 끊김 | DNS 레코드 재설정하고, Route 53 또는 유사 서비스 확인함, 캐시 지우기 적용함 | * 확인방법: `nslookup` 명령으로 해석 테스트<br>* References: [AWS Route 53 Troubleshooting](https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/troubleshooting.html) - AWS Route 53 문제 해결 |
| **인증서 만료 오류 로그** | SCP 콘솔 LB 로그에서 인증서 관련 오류 확인 또는 `openssl x509 -in <cert-file> -noout -dates` | 정상: 인증서 만료 오류 없음<br>취약: 만료 오류(예: certificate expired) 로그 존재 | 인증서 만료 시 SSL 연결 거부됨, 보안 경고 발생으로 사용자 신뢰 저하됨 | 인증서 자동 갱신 설정하고, 만료일 모니터링 도구 도입함 | * 설정방법: SCP 콘솔에서 인증서 업로드<br>* References: [Certbot Auto-Renewal](https://certbot.eff.org/docs/using.html#renewing-certificates) - Certbot 인증서 갱신 가이드 |
| **리소스 한도 초과 오류** | SCP 콘솔 메트릭스에서 리소스 사용량 확인 또는 `kubectl top pods` 실행 | 정상: 리소스 초과 오류 없음<br>취약: 초과 오류(예: resource limit exceeded) 존재 | 리소스 초과 시 파드 종료됨, LB 안정성 저하로 서비스 불안정함 | 리소스 요청/한도 증가시키고, HPA 설정으로 자동 스케일링 적용함 | * 확인방법: `kubectl describe pod`로 리소스 이벤트 확인<br>* References: [Kubernetes Resource Limits](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/) - Kubernetes 리소스 관리 |
| **구성 파싱 오류 로그** | SCP 콘솔 로그에서 구성 오류 확인 또는 `kubectl apply -f <yaml> --dry-run=server` 테스트 | 정상: 파싱 오류 없음<br>취약: 파싱 오류(예: invalid configuration) 로그 존재 | 구성 파싱 실패 시 LB 적용되지 않음, 배포 오류로 서비스 중단됨 | YAML 파일 검증 도구 사용하고, 구성 재작성함 | * 설정방법: `kubectl apply` 전에 dry-run 테스트<br>* References: [Kubernetes YAML Validation](https://kubernetes.io/docs/tasks/manage-kubernetes-objects/declarative-config/) - Kubernetes 선언적 구성 |
| **SCP API 호출 실패 오류** | SCP 콘솔 또는 API 로그에서 호출 실패 확인 | 정상: API 호출 실패 오류 없음<br>취약: 실패(예: API error 500) 오류 존재 | API 실패 시 LB 관리 불가함, 자동화 스크립트 오류로 운영 지연됨 | API 인증 키 재발급하고, 네트워크 연결 확인함 | * 확인방법: SCP API 클라이언트 로그 분석<br>* References: [AWS API Troubleshooting](https://docs.aws.amazon.com/apigateway/latest/developerguide/api-gateway-troubleshooting.html) - AWS API 문제 해결 |
| **파드 크래시 루프 오류** | `kubectl describe pod <pod-name>`로 크래시 루프 확인 | 정상: 크래시 루프 오류 없음<br>취약: 크래시 루프(예: CrashLoopBackOff) 존재 | 파드 반복 크래시 시 백엔드 불안정함, LB 트래픽 분배 실패함 | 파드 로그 분석하고, 애플리케이션 버그 수정함, 리소스 할당 증가함 | * 확인방법: `kubectl logs -p <pod>`로 이전 로그 확인<br>* References: [Kubernetes CrashLoopBackOff](https://kubernetes.io/docs/tasks/debug/debug-application/determine-reason-pod-failure/) - Kubernetes 파드 실패 이유 |
| **네트워크 파티션 오류 로그** | SCP 콘솔 네트워크 로그 확인 또는 `kubectl get nodes`로 노드 상태 확인 | 정상: 파티션 오류 없음<br>취약: 파티션(예: network partition) 오류 존재 | 네트워크 파티션 시 노드 간 통신 끊김, LB 동기화 실패로 데이터 불일치함 | 네트워크 토폴로지 재설계하고, 다중 AZ 배포함 | * 설정방법: SCP 콘솔에서 AZ 설정<br>* References: [Kubernetes Networking](https://kubernetes.io/docs/concepts/cluster-administration/networking/) - Kubernetes 네트워킹 |
| **높은 지연 시간 로그** | SCP 콘솔 메트릭스에서 지연 로그 확인 또는 `kubectl top nodes` | 정상: 높은 지연 로그 없음<br>취약: 지연(예: high latency) 로그 존재 | 지연 증가 시 응답 시간 길어짐, 사용자 불만 증가함 | 네트워크 최적화하고, CDN 통합함 | * 확인방법: Prometheus 쿼리로 지연 메트릭스<br>* References: [Prometheus Monitoring](https://prometheus.io/docs/prometheus/latest/querying/basics/) - Prometheus 쿼리 기본 |
| **패킷 손실 지표 확인** | SCP 콘솔 또는 `ping <lb-ip> -c 100`로 패킷 손실 테스트 | 정상: 패킷 손실 0%<br>취약: 패킷 손실 >0% | 패킷 손실 시 데이터 누락됨, 연결 불안정으로 서비스 품질 저하됨 | 네트워크 하드웨어 점검하고, QoS 설정 적용함 | * 확인방법: `mtr <lb-ip>`로 경로 추적<br>* References: [Packet Loss Troubleshooting](https://www.cloudflare.com/learning/network-layer/what-is-packet-loss/) - 패킷 손실 설명 |
| **인증 실패 오류 로그** | SCP 콘솔 로그에서 인증 오류 확인 | 정상: 인증 실패 오류 없음<br>취약: 실패(예: authentication failed) 로그 존재 | 인증 실패 시 무단 접근 시도 증가함, 보안 침해 위험함 | IAM 역할 재설정하고, MFA 활성화함 | * 설정방법: SCP 콘솔에서 IAM 정책 수정<br>* References: [AWS IAM Best Practices](https://docs.aws.amazon.com/IAM/latest/UserGuide/best-practices.html) - AWS IAM 베스트 프랙티스 |
| **속도 제한 오류 확인** | SCP 콘솔 로그에서 rate limit 오류 확인 | 정상: rate limit 오류 없음<br>취약: 오류(예: 429 Too Many Requests) 존재 | 속도 제한 초과 시 API 호출 차단됨, 자동화 작업 중단됨 | 요청 속도 조절하고, 배치 처리 적용함 | * 확인방법: API 응답 헤더 분석<br>* References: [Rate Limiting](https://developer.mozilla.org/en-US/docs/Glossary/Rate_limiting) - 속도 제한 설명 |
| **백엔드 과부하 오류** | SCP 콘솔 메트릭스에서 과부하 로그 확인 | 정상: 과부하 오류 없음<br>취약: 과부하(예: overload error) 존재 | 과부하 시 백엔드 다운됨, LB failover 실패로 서비스 중단됨 | 로드 밸런싱 알고리즘 최적화하고, 스케일 아웃함 | * 설정방법: HPA 설정<br>* References: [Kubernetes HPA](https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/) - Kubernetes HPA |
| **페일오버 실패 로그** | SCP 콘솔 로그에서 failover 오류 확인 | 정상: failover 실패 없음<br>취약: 실패(예: failover failed) 로그 존재 | failover 실패 시 단일 장애점 발생함, 고가용성 상실됨 | 다중 LB 인스턴스 설정하고, 헬스 체크 강화함 | * 확인방법: SCP 콘솔에서 failover 테스트<br>* References: [High Availability Load Balancing](https://www.nginx.com/resources/glossary/high-availability-load-balancing/) - 고가용성 LB 설명 |
| **LB 인스턴스 동기화 오류** | SCP 콘솔 로그에서 sync 오류 확인 | 정상: sync 오류 없음<br>취약: 오류(예: sync failed) 존재 | 동기화 실패 시 구성 불일치함, 트래픽 분배 오류 발생함 | 클러스터 동기화 도구 사용하고, 마스터-슬레이브 설정 확인함 | * 설정방법: Kubernetes 클러스터 재동기화<br>* References: [Kubernetes Cluster Federation](https://kubernetes.io/docs/tasks/federation/) - Kubernetes 페더레이션 |
| **메모리 누출 오류 로그** | SCP 콘솔 또는 `kubectl top pods`로 메모리 사용 추적 | 정상: 메모리 누출 없음<br>취약: 누출(예: memory leak) 로그 존재 | 메모리 누출 시 파드 OOMKilled 됨, 반복 재시작으로 불안정함 | 애플리케이션 코드 프로파일링하고, 가비지 컬렉션 최적화함 | * 확인방법: Heapster 또는 Prometheus로 모니터링<br>* References: [Memory Leak Detection](https://www.jetbrains.com/help/profiler/Memory_Leaks.html) - 메모리 누출 탐지 |
| **CPU 스파이크 오류** | SCP 콘솔 메트릭스에서 CPU 로그 확인 | 정상: CPU 스파이크 오류 없음<br>취약: 스파이크(예: high CPU usage) 존재 | CPU 스파이크 시 응답 지연됨, 다른 프로세스 영향받음 | CPU 한도 설정하고, 워크로드 분산함 | * 확인방법: `kubectl top nodes`<br>* References: [Kubernetes CPU Management](https://kubernetes.io/docs/tasks/administer-cluster/manage-resources/cpu-default-namespace/) - Kubernetes CPU 관리 |
| **디스크 I/O 오류 로그** | SCP 콘솔 로그에서 I/O 오류 확인 또는 `iostat` 명령 | 정상: I/O 오류 없음<br>취약: 오류(예: disk I/O error) 존재 | I/O 오류 시 로그 쓰기 실패함, 데이터 손실 위험함 | 스토리지 볼륨 업그레이드하고, RAID 설정 적용함 | * 확인방법: `dmesg | grep error`<br>* References: [Disk I/O Troubleshooting](https://www.linode.com/docs/guides/troubleshooting-disk-io/) - 디스크 I/O 문제 해결 |
| **펌웨어 버그 관련 오류** | SCP 콘솔 시스템 로그에서 펌웨어 오류 확인 | 정상: 펌웨어 버그 오류 없음<br>취약: 버그(예: firmware bug) 로그 존재 | 펌웨어 버그 시 하드웨어 불안정함, LB 성능 저하됨 | 펌웨어 업데이트 적용하고, 벤더 패치 확인함 | * 설정방법: SCP 콘솔에서 업데이트 관리<br>* References: [Firmware Update Best Practices](https://www.cisco.com/c/en/us/support/docs/servers-unified-computing/ucs-manager/110511-ucs-fw-mgmt-00.html) - 펌웨어 업데이트 가이드 |
| **소프트웨어 버전 비호환 오류** | SCP 콘솔 또는 `kubectl version`으로 버전 확인 | 정상: 비호환 오류 없음<br>취약: 비호환(예: version mismatch) 오류 존재 | 버전 비호환 시 API 호출 실패함, 기능 동작 안 함 | 모든 컴포넌트 버전 업그레이드하고, 호환성 테스트함 | * 확인방법: `kubectl get nodes -o wide`<br>* References: [Kubernetes Version Skew](https://kubernetes.io/docs/setup/release/version-skew-policy/) - Kubernetes 버전 정책 |
| **K8s 어노테이션 구성 오류** | `kubectl get svc -o yaml`로 어노테이션 확인 및 로그 분석 | 정상: 어노테이션 오류 없음<br>취약: 오류(예: invalid annotation) 존재 | 어노테이션 오류 시 LB 기능 적용 안 됨, 예상치 못한 동작 발생함 | 어노테이션 문법 검증하고, 공식 문서 준수함 | * 설정방법: `kubectl annotate svc <name> key=value`<br>* References: [Kubernetes Annotations](https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/) - Kubernetes 어노테이션 |

## 가용성

| 점검 내용 | 점검 방법 | 점검 기준 | 취약 시 문제점 | 개선 방안 | 참고 |
|-----------|-----------|-----------|---------------|-----------|-------|
| **다중 AZ 배포 설정** | SCP 콘솔에서 LB 배포 AZ 확인 또는 `kubectl get svc -o yaml`로 AZ 어노테이션 확인 | 정상: LB가 2개 이상의 AZ에 배포됨<br>취약: 단일 AZ 배포 | 단일 AZ 장애 시 전체 LB 다운됨, 서비스 중단 발생함, 가용성 저하됨 | LB를 다중 AZ에 배포하고, 자동 failover 설정함, 트래픽 분산을 위해 AZ 균형 유지함 | * 확인방법: SCP 콘솔 AZ 섹션 확인 또는 `kubectl get nodes -o wide`로 AZ 분포 확인<br>* References: [AWS Multi-AZ Deployment](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/application-load-balancers.html#availability-zones) - AWS ALB 다중 AZ 설정 가이드 |
| **헬스 체크 활성화** | SCP 콘솔에서 헬스 체크 설정 확인 또는 `kubectl describe svc <service-name>` 실행 | 정상: 헬스 체크가 활성화되고 경로/포트 설정됨<br>취약: 헬스 체크 비활성화 | 비정상 백엔드가 트래픽 수신함, 서비스 불안정함, 장애 확산됨 | 헬스 체크 경로를 `/health`로 설정하고, 주기 10초, 임계값 3회로 활성화함, 백엔드 상태 정확 반영함 | * 설정방법: SCP 콘솔 헬스 체크 섹션 수정 또는 `service.spec.healthCheckNodePort` 설정<br>* References: [Kubernetes Health Checks](https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/) - Kubernetes 헬스 체크 프로브 설명 |
| **Failover 메커니즘 설정** | SCP 콘솔에서 failover 설정 확인 또는 `kubectl get ingress -o yaml`로 어노테이션 확인 | 정상: 자동 failover가 활성화됨<br>취약: failover 미설정 | LB 장애 시 수동 개입 필요함, 다운타임 증가함, 가용성 저하됨 | 다중 LB 인스턴스 설정하고, 헬스 체크 기반 자동 failover 활성화함, 테스트를 통해 검증함 | * 확인방법: SCP 콘솔 failover 섹션 또는 `kubectl describe ingress`<br>* References: [Azure Load Balancer Failover](https://learn.microsoft.com/en-us/azure/load-balancer/load-balancer-overview) - Azure LB failover 설명 |
| **Auto Scaling 통합** | SCP 콘솔에서 autoscaling 설정 확인 또는 `kubectl get hpa` 실행 | 정상: HPA가 LB와 연동됨<br>취약: autoscaling 미설정 | 트래픽 급증 시 과부하 발생함, 서비스 중단 위험 증가함 | HPA를 CPU 50% 기준으로 설정하고, min 2, max 10으로 LB 백엔드 스케일링함 | * 설정방법: `kubectl autoscale deployment <name> --cpu-percent=50 --min=2 --max=10`<br>* References: [Kubernetes Horizontal Pod Autoscaler](https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/) - Kubernetes HPA 가이드 |
| **백업 및 복구 정책** | SCP 콘솔에서 백업 설정 확인 또는 스냅샷 로그 확인 | 정상: 정기 백업이 설정됨<br>취약: 백업 미설정 | 장애 시 데이터 손실됨, 복구 지연됨, 가용성 저하됨 | 일일 백업 스케줄 설정하고, 복구 테스트 수행함, 스토리지 스냅샷 사용함 | * 확인방법: SCP 콘솔 백업 섹션 또는 `kubectl get pv -o yaml`<br>* References: [AWS Backup for ELB](https://docs.aws.amazon.com/aws-backup/latest/devguide/whatis.html) - AWS 백업 서비스 설명 |
| **모니터링 및 알림 설정** | SCP 콘솔에서 모니터링 대시보드 확인 또는 `kubectl get prometheusrules` | 정상: 메트릭스 모니터링과 알림이 활성화됨<br>취약: 모니터링 미설정 | 장애 조기 발견 불가함, 다운타임 증가함 | Prometheus 통합하고, CPU/메모리 임계값 알림 설정함, 슬랙이나 이메일 연동함 | * 설정방법: SCP 콘솔 모니터링 섹션 활성화<br>* References: [Prometheus Alerting](https://prometheus.io/docs/prometheus/latest/configuration/alerting_rules/) - Prometheus 알림 규칙 가이드 |
| **리던던시 구성** | SCP 콘솔에서 LB 인스턴스 수 확인 또는 `kubectl get replicasets` | 정상: 최소 2개 이상의 LB 인스턴스<br>취약: 단일 인스턴스 | 단일 장애점으로 서비스 중단됨, 가용성 취약함 | LB를 active-active 또는 active-passive로 구성하고, 리던던시 테스트함 | * 확인방법: `kubectl get pods -l app=lb`<br>* References: [High Availability in Kubernetes](https://kubernetes.io/docs/setup/production-environment/tools/kubeadm/high-availability/) - Kubernetes 고가용성 설정 |
| **백엔드 최소 인스턴스 수** | SCP 콘솔에서 백엔드 풀 확인 또는 `kubectl get deployments -o yaml` | 정상: 최소 2개 이상의 백엔드 파드<br>취약: 단일 백엔드 | 백엔드 장애 시 전체 서비스 다운됨, SPOF 발생함 | Deployment replicas를 2 이상으로 설정하고, anti-affinity 규칙 적용함 | * 설정방법: `kubectl scale deployment <name> --replicas=2`<br>* References: [Kubernetes ReplicaSet](https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/) - Kubernetes ReplicaSet 설명 |
| **세션 어피니티 활성화** | SCP 콘솔에서 세션 설정 확인 또는 `kubectl get svc -o yaml`로 sessionAffinity 확인 | 정상: sessionAffinity=ClientIP 설정됨<br>취약: 세션 어피니티 미설정 | 세션 불일치로 애플리케이션 오류 발생함, 사용자 세션 손실됨 | ClientIP 기반 세션 어피니티 설정하고, 타임아웃 값 조정함 | * 설정방법: `service.spec.sessionAffinity=ClientIP`<br>* References: [Kubernetes Session Affinity](https://kubernetes.io/docs/concepts/services-networking/service/#session-affinity) - Kubernetes 세션 어피니티 가이드 |
| **외부 트래픽 정책 설정** | `kubectl get svc -o yaml`로 externalTrafficPolicy 확인 | 정상: externalTrafficPolicy=Local 설정됨<br>취약: Cluster 모드 | 노드 장애 시 트래픽 손실됨, 가용성 저하됨 | Local 모드로 설정하고, 헬스 체크 강화함, 노드 간 트래픽 최소화함 | * 설정방법: `service.spec.externalTrafficPolicy=Local`<br>* References: [Kubernetes External Traffic Policy](https://kubernetes.io/docs/tasks/access-application-cluster/create-external-load-balancer/#external-traffic-policy) - Kubernetes 외부 트래픽 정책 설명 |
| **SSL 오프로딩 설정** | SCP 콘솔에서 SSL 설정 확인 또는 `kubectl get ingress -o yaml` | 정상: LB에서 SSL termination 활성화됨<br>취약: 백엔드에서 SSL 처리 | 백엔드 부하 증가함, 장애 시 복잡한 복구 필요함 | LB에서 SSL 오프로딩 설정하고, 인증서 관리 자동화함 | * 확인방법: SCP 콘솔 SSL 섹션<br>* References: [AWS ALB SSL Offloading](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/create-https-listener.html) - AWS ALB SSL 리스너 설정 |
| **로그 활성화 및 보관** | SCP 콘솔에서 로그 설정 확인 | 정상: 액세스 로그가 활성화되고 보관 기간 7일 이상<br>취약: 로그 비활성화 | 장애 분석 불가함, 문제 원인 파악 지연됨 | 로그를 S3나 유사 스토리지에 저장하고, 보관 기간 설정함 | * 설정방법: SCP 콘솔 로그 섹션 활성화<br>* References: [AWS ELB Access Logs](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-access-logs.html) - AWS ELB 액세스 로그 가이드 |
| **보안 그룹 리던던시** | SCP 콘솔에서 보안 그룹 확인 | 정상: 보안 그룹이 리던던트하게 설정됨<br>취약: 단일 보안 그룹 | 보안 그룹 변경 시 전체 영향받음, 가용성 저하됨 | 다중 보안 그룹 적용하고, 최소 권한 원칙 준수함 | * 확인방법: SCP 콘솔 보안 그룹 섹션<br>* References: [AWS Security Groups](https://docs.aws.amazon.com/vpc/latest/userguide/VPC_SecurityGroups.html) - AWS 보안 그룹 가이드 |
| **DNS Failover 설정** | SCP 콘솔 또는 DNS 툴에서 failover 레코드 확인 | 정상: DNS failover가 활성화됨<br>취약: failover 미설정 | LB 장애 시 DNS 해석 실패함, 서비스 접근 불가함 | Route 53 failover 라우팅 정책 설정하고, 헬스 체크 연동함 | * 확인방법: `dig <domain>`<br>* References: [AWS Route 53 Failover](https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/routing-policy.html#routing-policy-failover) - AWS Route 53 failover 정책 |
| **리소스 한도 설정** | `kubectl describe pod`로 limits 확인 | 정상: CPU/메모리 limits 설정됨<br>취약: limits 미설정 | 리소스 과다 사용 시 노드 다운됨, 클러스터 불안정함 | requests/limits를 적절히 설정하고, QoS Guaranteed 적용함 | * 설정방법: pod.spec.resources.limits<br>* References: [Kubernetes Resource Management](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/) - Kubernetes 리소스 관리 가이드 |
| **패치 관리 정책** | SCP 콘솔에서 패치 로그 확인 또는 `kubectl get nodes` 버전 확인 | 정상: 최신 패치 적용됨<br>취약: 패치 미적용 | 취약점 노출됨, 장애 발생 위험 증가함 | 자동 패치 설정하고, 롤링 업데이트 수행함 | * 확인방법: `kubelet --version`<br>* References: [Kubernetes Upgrades](https://kubernetes.io/docs/tasks/administer-cluster/cluster-upgrade/) - Kubernetes 클러스터 업그레이드 가이드 |
| **버전 호환성 확인** | `kubectl version`으로 버전 확인 | 정상: 모든 컴포넌트 버전 호환됨<br>취약: 버전 불일치 | API 비호환으로 장애 발생함, 가용성 저하됨 | 버전 skew 정책 준수하고, 업그레이드 계획 수립함 | * 확인방법: `kubectl get nodes -o wide`<br>* References: [Kubernetes Version Skew Policy](https://kubernetes.io/docs/setup/release/version-skew-policy/) - Kubernetes 버전 정책 |
| **네트워크 리던던시** | SCP 콘솔에서 네트워크 구성 확인 | 정상: 다중 네트워크 경로 설정됨<br>취약: 단일 경로 | 네트워크 장애 시 연결 끊김, 서비스 중단됨 | VPC peering 또는 다중 NIC 설정함 | * 확인방법: SCP 콘솔 네트워크 섹션<br>* References: [AWS VPC Networking](https://docs.aws.amazon.com/vpc/latest/userguide/what-is-amazon-vpc.html) - AWS VPC 네트워킹 가이드 |
| **캐싱 통합** | SCP 콘솔에서 캐시 설정 확인 또는 `kubectl get configmaps` | 정상: Redis나 Memcached 통합됨<br>취약: 캐싱 미설정 | 반복 요청으로 부하 증가함, 응답 지연됨 | 캐시 레이어 추가하고, TTL 설정함 | * 설정방법: StatefulSet으로 캐시 배포<br>* References: [Redis in Kubernetes](https://redis.io/docs/management/kubernetes/) - Redis Kubernetes 배포 가이드 |
| **CDN 통합 설정** | SCP 콘솔에서 CDN 설정 확인 | 정상: CDN이 LB와 연동됨<br>취약: CDN 미설정 | 글로벌 트래픽 지연됨, 가용성 지역적 제한됨 | CloudFront나 유사 CDN 설정하고, 오리진으로 LB 지정함 | * 확인방법: SCP 콘솔 CDN 섹션<br>* References: [AWS CloudFront](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/Introduction.html) - AWS CloudFront 소개 |
| **WAF 활성화** | SCP 콘솔에서 WAF 설정 확인 | 정상: WAF가 LB와 통합됨<br>취약: WAF 미설정 | DDoS 공격으로 서비스 다운됨, 가용성 저하됨 | OWASP 규칙 적용하고, rate limiting 설정함 | * 설정방법: SCP 콘솔 WAF 섹션<br>* References: [AWS WAF](https://docs.aws.amazon.com/waf/latest/developerguide/waf-chapter.html) - AWS WAF 가이드 |
| **타임아웃 최적화** | SCP 콘솔에서 타임아웃 값 확인 | 정상: 연결 타임아웃 60초, 유휴 300초<br>취약: 부적절한 값 | 연결 끊김 또는 리소스 낭비됨, 장애 발생함 | 애플리케이션 요구에 맞게 타임아웃 조정함 | * 설정방법: SCP 콘솔 타임아웃 섹션 수정<br>* References: [AWS ALB Timeout](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/application-load-balancers.html#connection-idle-timeout) - AWS ALB 타임아웃 설정 |
| **연결 제한 설정** | SCP 콘솔에서 연결 제한 확인 | 정상: 적절한 최대 연결 수 설정됨<br>취약: 제한 미설정 | 과다 연결로 과부하됨, 서비스 거부됨 | 최대 연결 수를 워크로드에 맞게 설정하고, 모니터링함 | * 확인방법: SCP 콘솔 연결 섹션<br>* References: [Azure LB Limits](https://learn.microsoft.com/en-us/azure/load-balancer/load-balancer-limits) - Azure LB 제한 설명 |
| **헬스 체크 임계값** | SCP 콘솔에서 임계값 확인 | 정상: 실패 임계값 3회, 성공 2회<br>취약: 부적절한 임계값 | 잘못된 백엔드 제외 또는 포함됨, 가용성 불안정함 | 임계값을 최적화하고, 테스트함 | * 설정방법: SCP 콘솔 헬스 체크 섹션 수정<br>* References: [Azure LB Health Probes](https://learn.microsoft.com/en-us/azure/load-balancer/load-balancer-custom-probe-overview) - Azure LB 헬스 프로브 가이드 |
| **메트릭스 기반 알림** | SCP 콘솔에서 알림 규칙 확인 | 정상: Latency, Error Rate 알림 설정됨<br>취약: 알림 미설정 | 성능 저하 시 늦은 대응함, 다운타임 증가함 | CloudWatch나 유사 도구로 메트릭스 알림 설정함 | * 확인방법: SCP 콘솔 메트릭스 섹션<br>* References: [AWS CloudWatch Alarms](https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/AlarmThatSendsEmail.html) - AWS CloudWatch 알람 가이드 |

## 성능 및 용량

| 점검 내용 | 점검 방법 | 점검 기준 | 취약 시 문제점 | 개선 방안 | 참고 |
|-----------|-----------|-----------|---------------|-----------|-------|
| **CPU 사용량 모니터링** | SCP 콘솔 메트릭스 대시보드에서 LB CPU 사용량 확인 또는 `kubectl top pods -l app=lb` 실행 | 정상: 평균 CPU 사용량 70% 미만<br>취약: 평균 CPU 사용량 70% 이상 | CPU 과부하로 인해 요청 처리 지연 발생함, LB 응답 시간이 증가함, 전체 시스템 성능 저하됨 | LB 인스턴스 스케일 아웃 적용하고, CPU 한도 증가시킴, 워크로드 최적화로 불필요한 프로세스 제거함 | * 확인방법: Prometheus 쿼리로 CPU 메트릭스 분석<br>* 설정방법: `kubectl autoscale`로 HPA 설정<br>* References: [Kubernetes CPU Management](https://kubernetes.io/docs/tasks/administer-cluster/manage-resources/cpu-default-namespace/) - Kubernetes CPU 관리 가이드 |
| **메모리 사용량 확인** | SCP 콘솔에서 LB 메모리 메트릭스 확인 또는 `kubectl top pods` 실행 | 정상: 메모리 사용량 80% 미만<br>취약: 메모리 사용량 80% 이상 | 메모리 부족 시 OOMKilled 발생함, LB 재시작으로 서비스 중단됨, 성능 불안정함 | 메모리 한도 증가시키고, 메모리 누출 코드 수정함, 가비지 컬렉션 튜닝 적용함 | * 확인방법: `kubectl describe pod`로 메모리 이벤트 로그 확인<br>* 설정방법: pod.spec.resources.limits.memory 설정<br>* References: [Kubernetes Resource Limits](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/) - Kubernetes 리소스 관리 설명 |
| **네트워크 대역폭 용량** | SCP 콘솔 네트워크 메트릭스에서 대역폭 사용량 확인 또는 `kubectl exec -it <pod> -- iperf -c <target>` 테스트 | 정상: 대역폭 사용량 80% 미만<br>취약: 대역폭 사용량 80% 이상 | 대역폭 포화 시 패킷 손실 발생함, 지연 시간 증가함, 트래픽 처리 용량 초과로 서비스 저하됨 | 네트워크 인터페이스 업그레이드하고, 트래픽 쉐이핑 적용함, CDN 통합으로 외부 트래픽 분산함 | * 확인방법: `vnstat` 또는 Prometheus 네트워크 메트릭스<br>* 설정방법: SCP 콘솔에서 네트워크 타입 변경<br>* References: [AWS Networking Best Practices](https://docs.aws.amazon.com/whitepapers/latest/aws-networking-best-practices/aws-networking-best-practices.html) - AWS 네트워킹 베스트 프랙티스 |
| **동시 연결 수 제한** | SCP 콘솔에서 연결 메트릭스 확인 또는 `kubectl describe svc`로 연결 설정 확인 | 정상: 최대 연결 수 설정 및 현재 연결 70% 미만<br>취약: 연결 수 초과 또는 미설정 | 연결 초과 시 새로운 요청 거부됨, 서비스 거부 상태 발생함, 용량 부족으로 사용자 불만 증가함 | 최대 연결 수 증가시키고, rate limiting 적용함, 백엔드 풀 확장으로 분산 처리함 | * 확인방법: `netstat -an | grep ESTABLISHED | wc -l`<br>* 설정방법: SCP 콘솔 연결 제한 섹션 수정<br>* References: [Azure Load Balancer Limits](https://learn.microsoft.com/en-us/azure/load-balancer/load-balancer-limits) - Azure LB 제한 설명 |
| **요청 지연 시간 (Latency)** | SCP 콘솔 메트릭스에서 latency 그래프 확인 또는 `kubectl exec -it <pod> -- curl -w "%{time_total}" <lb-url>` | 정상: 평균 latency 100ms 미만<br>취약: 평균 latency 100ms 이상 | 지연 증가 시 사용자 응답 대기 시간 길어짐, 애플리케이션 성능 저하됨, 경쟁력 상실됨 | 네트워크 최적화하고, 캐싱 레이어 추가함, 지리적 가까운 AZ 배포함 | * 확인방법: Prometheus histogram 쿼리<br>* 설정방법: Ingress 어노테이션으로 latency 최적화<br>* References: [Kubernetes Performance Tuning](https://kubernetes.io/docs/tasks/administer-cluster/manage-resources/) - Kubernetes 성능 튜닝 가이드 |
| **Throughput 용량** | SCP 콘솔에서 throughput 메트릭스 확인 또는 `kubectl exec -it <pod> -- ab -n 1000 -c 100 <lb-url>` 벤치마크 | 정상: throughput 80% 미만 용량 사용<br>취약: throughput 80% 이상 | throughput 초과 시 병목 현상 발생함, 데이터 전송 속도 저하됨, 대용량 트래픽 처리 실패함 | LB 인스턴스 타입 업그레이드하고, 압축 활성화함, 멀티패스 네트워킹 적용함 | * 확인방법: Apache Benchmark(ab) 도구 사용<br>* 설정방법: SCP 콘솔 throughput 설정<br>* References: [AWS ELB Performance](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-cloudwatch-metrics.html) - AWS ELB 성능 메트릭스 설명 |
| **백엔드 인스턴스 용량** | SCP 콘솔에서 백엔드 풀 크기 확인 또는 `kubectl get pods -l app=backend` | 정상: 백엔드 파드 수 트래픽에 비례 (최소 3개)<br>취약: 부족한 파드 수 | 인스턴스 부족 시 과부하 발생함, 성능 저하 및 장애 확산됨, 용량 초과로 스케일링 지연됨 | HPA 설정으로 자동 스케일링 적용하고, min replicas 증가함 | * 확인방법: `kubectl get hpa`<br>* 설정방법: `kubectl autoscale deployment <name> --min=3`<br>* References: [Kubernetes HPA](https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/) - Kubernetes 수평 팟 오토스케일러 가이드 |
| **스케일링 지연 시간** | SCP 콘솔에서 스케일링 메트릭스 확인 또는 HPA 이벤트 로그 확인 | 정상: 스케일링 지연 1분 미만<br>취약: 스케일링 지연 1분 이상 | 트래픽 급증 시 지연으로 성능 저하됨, 용량 부족 기간 동안 서비스 불안정함 | HPA 쿨다운 기간 단축하고, 예측 스케일링 도입함 | * 확인방법: `kubectl describe hpa`<br>* 설정방법: HPA spec.behavior.scaleUp.stabilizationWindowSeconds 조정<br>* References: [Kubernetes Scaling Behaviors](https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/#configurable-scaling-behavior) - Kubernetes 스케일링 동작 설정 |
| **헬스 체크 지연** | SCP 콘솔 헬스 체크 메트릭스 확인 또는 `kubectl describe svc` | 정상: 헬스 체크 주기 10초 미만<br>취약: 헬스 체크 주기 10초 이상 | 지연된 헬스 체크로 비정상 백엔드 트래픽 수신함, 성능 저하됨, 용량 낭비됨 | 헬스 체크 주기 5초로 단축하고, 임계값 최적화함 | * 확인방법: SCP 콘솔 헬스 체크 섹션<br>* 설정방법: service.spec.healthCheckNodePort 설정<br>* References: [Azure LB Health Probes](https://learn.microsoft.com/en-us/azure/load-balancer/load-balancer-custom-probe-overview) - Azure LB 헬스 프로브 개요 |
| **타임아웃 값 최적화** | SCP 콘솔에서 타임아웃 설정 확인 | 정상: 연결 타임아웃 60초, 유휴 300초<br>취약: 부적절한 타임아웃 값 | 짧은 타임아웃으로 연결 끊김 발생함, 긴 타임아웃으로 리소스 낭비됨, 성능 영향 받음 | 애플리케이션 요구에 맞게 타임아웃 조정하고, 모니터링함 | * 확인방법: SCP 콘솔 타임아웃 섹션<br>* 설정방법: LB 리스너 타임아웃 수정<br>* References: [AWS ALB Idle Timeout](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/application-load-balancers.html#connection-idle-timeout) - AWS ALB 유휴 타임아웃 설정 |
| **트래픽 분배 알고리즘** | SCP 콘솔에서 알고리즘 설정 확인 또는 어노테이션 확인 | 정상: Least Connections 또는 적절 알고리즘<br>취약: Round Robin만 사용 | 불균형 분배로 특정 인스턴스 과부하됨, 성능 불균형 발생함, 용량 효율 저하됨 | Least Connections로 변경하고, 세션 어피니티 결합함 | * 확인방법: `kubectl get svc -o yaml`<br>* 설정방법: Ingress 어노테이션으로 알고리즘 지정<br>* References: [Load Balancing Algorithms](https://www.nginx.com/resources/glossary/load-balancing/) - NGINX 로드 밸런싱 알고리즘 설명 |
| **캐싱 설정 활성화** | SCP 콘솔에서 캐시 설정 확인 또는 ConfigMap 확인 | 정상: 캐싱 활성화됨<br>취약: 캐싱 미설정 | 반복 요청으로 백엔드 부하 증가함, 응답 시간 지연됨, 성능 저하됨 | Redis 캐시 통합하고, TTL 설정함, 정적 콘텐츠 캐싱 적용함 | * 확인방법: `kubectl get configmaps`<br>* 설정방법: Sidecar 패턴으로 캐시 추가<br>* References: [Redis Caching in Kubernetes](https://redis.io/docs/management/kubernetes/) - Redis Kubernetes 배포 가이드 |
| **데이터 압축 활성화** | SCP 콘솔에서 압축 설정 확인 또는 `kubectl get ingress -o yaml` | 정상: GZIP 압축 활성화됨<br>취약: 압축 미활성화 | 대역폭 낭비로 전송 속도 저하됨, 모바일 사용자 성능 영향 받음 | LB에서 GZIP 압축 설정하고, 콘텐츠 타입 지정함 | * 확인방법: curl -I -H "Accept-Encoding: gzip" <url><br>* 설정방법: Ingress 어노테이션 nginx.ingress.kubernetes.io/enable-compression<br>* References: [NGINX Compression](https://docs.nginx.com/nginx/admin-guide/web-server/compression/) - NGINX 압축 설정 가이드 |
| **SSL 오프로딩 효율** | SCP 콘솔 SSL 메트릭스 확인 또는 성능 테스트 | 정상: LB에서 SSL termination<br>취약: 백엔드 SSL 처리 | 백엔드 CPU 부하 증가함, 전체 처리 속도 저하됨, 용량 소모됨 | LB에서 SSL 오프로딩 활성화하고, cipher 최적화함 | * 확인방법: SCP 콘솔 SSL 섹션<br>* 설정방법: LB 리스너 HTTPS 설정<br>* References: [AWS ALB SSL Termination](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/create-https-listener.html) - AWS ALB HTTPS 리스너 생성 |
| **로깅 오버헤드 최소화** | SCP 콘솔 로그 메트릭스 확인 또는 로그 볼륨 확인 | 정상: 로그 레벨 INFO, 샘플링 적용<br>취약: 상세 로그 활성화 | 로깅으로 디스크/CPU 소모 증가함, 성능 저하됨, 저장 용량 초과됨 | 로그 레벨 조정하고, 샘플링 로깅 적용함, 외부 로그 저장소 사용함 | * 확인방법: `kubectl logs` 크기 확인<br>* 설정방법: Fluentd 또는 ELK 스택 통합<br>* References: [Kubernetes Logging](https://kubernetes.io/docs/concepts/cluster-administration/logging/) - Kubernetes 로깅 개념 |
| **모니터링 메트릭스 세분화** | SCP 콘솔 메트릭스 설정 확인 또는 Prometheus 확인 | 정상: 커스텀 메트릭스 활성화<br>취약: 기본 메트릭스만 | 세부 성능 문제 파악 어려움, 최적화 지연됨, 용량 계획 오류 발생함 | Prometheus 커스텀 메트릭스 추가하고, Grafana 대시보드 설정함 | * 확인방법: `kubectl get prometheusrules`<br>* 설정방법: ServiceMonitor CRD 생성<br>* References: [Prometheus Kubernetes Monitoring](https://prometheus.io/docs/prometheus/latest/configuration/configuration/) - Prometheus 설정 가이드 |
| **Pod 리소스 할당** | `kubectl describe pod`로 resources 확인 | 정상: requests/limits 적절 설정<br>취약: 미설정 또는 과소 | 리소스 경쟁으로 성능 저하됨, 노드 불안정함, 용량 낭비됨 | Guaranteed QoS로 requests=limits 설정하고, 테스트 기반 조정함 | * 확인방법: `kubectl top pods`<br>* 설정방법: pod.spec.resources.requests<br>* References: [Kubernetes QoS](https://kubernetes.io/docs/tasks/configure-pod-container/quality-service-pod/) - Kubernetes QoS 클래스 설명 |
| **Pod Affinity 규칙** | `kubectl get deployments -o yaml`로 affinity 확인 | 정상: anti-affinity 설정<br>취약: 미설정 | 동일 노드 집중으로 병목 발생함, 노드 장애 시 대규모 영향 받음 | podAntiAffinity 규칙 적용하고, 토폴로지 키 설정함 | * 확인방법: `kubectl describe pod` affinity 섹션<br>* 설정방법: deployment.spec.template.spec.affinity<br>* References: [Kubernetes Affinity](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/) - Kubernetes 팟 할당 가이드 |
| **네트워크 MTU 크기** | SCP 콘솔 네트워크 설정 확인 또는 `kubectl exec -it <pod> -- ip link show` | 정상: MTU 9000 (Jumbo Frames)<br>취약: MTU 1500 | 작은 MTU로 패킷 분할 증가함, 오버헤드 상승으로 throughput 저하됨 | Jumbo Frames 활성화하고, 모든 네트워크 구성 요소 일치시킴 | * 확인방법: `ping -M do -s 8972 <ip>` 테스트<br>* 설정방법: Calico 또는 네트워크 플러그인 MTU 설정<br>* References: [Kubernetes Networking Performance](https://kubernetes.io/docs/concepts/extend-kubernetes/compute-storage-net/network-plugins/) - Kubernetes 네트워크 플러그인 |
| **QoS 정책 설정** | SCP 콘솔에서 QoS 확인 또는 네트워크 정책 확인 | 정상: 트래픽 우선순위 설정<br>취약: QoS 미설정 | 중요 트래픽 지연됨, 성능 불균형 발생함, 용량 비효율 사용됨 | NetworkPolicy로 QoS 적용하고, DSCP 마킹함 | * 확인방법: `kubectl get networkpolicies`<br>* 설정방법: Calico QoS 프로파일 생성<br>* References: [Kubernetes Network Policies](https://kubernetes.io/docs/concepts/services-networking/network-policies/) - Kubernetes 네트워크 정책 설명 |
| **Rate Limiting 적용** | SCP 콘솔에서 rate limit 설정 확인 또는 Ingress 어노테이션 확인 | 정상: 클라이언트별 rate limit 설정<br>취약: 미설정 | 악의적 트래픽으로 과부하 발생함, 정상 사용자 성능 저하됨 | Ingress rate limit 어노테이션 설정하고, burst 허용함 | * 확인방법: `kubectl describe ingress`<br>* 설정방법: nginx.ingress.kubernetes.io/limit-rps<br>* References: [NGINX Rate Limiting](https://docs.nginx.com/nginx/admin-guide/security-controls/configuring-http-limit-requests/) - NGINX 요청 제한 설정 |
| **Burst 용량 관리** | SCP 콘솔 스케일링 메트릭스 확인 또는 HPA behavior 확인 | 정상: burst 스케일링 설정<br>취약: 미설정 | 갑작스러운 트래픽 증가 시 지연 발생함, 성능 저하됨 | HPA scaleUp policy로 burst 허용하고, stabilization window 조정함 | * 확인방법: `kubectl get hpa -o yaml`<br>* 설정방법: spec.behavior.scaleUp.policies<br>* References: [Kubernetes HPA Behaviors](https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/#configurable-scaling-behavior) - Kubernetes HPA 동작 설정 |
| **Warm-up 시간 설정** | SCP 콘솔에서 warm-up 확인 또는 deregistration delay 확인 | 정상: warm-up 30초 설정<br>취약: 미설정 또는 과장 | 새 인스턴스 즉시 트래픽 수신으로 초기 성능 저하됨, 에러 증가함 | LB deregistration delay 설정하고, readiness probe 연동함 | * 확인방법: SCP 콘솔 타겟 그룹 섹션<br>* 설정방법: LB target group attributes 수정<br>* References: [AWS ELB Deregistration Delay](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/target-group-deregistration-delay.html) - AWS ELB deregistration 지연 설명 |
| **Idle 연결 관리** | SCP 콘솔에서 idle timeout 확인 | 정상: idle timeout 60초<br>취약: 과장 또는 미설정 | 불필요한 연결 유지로 리소스 낭비됨, 용량 소모 증가함 | idle timeout 최적화하고, keep-alive 활성화함 | * 확인방법: curl -v <url>로 연결 확인<br>* 설정방법: LB 리스너 idle timeout 설정<br>* References: [AWS ALB Connection Idle Timeout](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/application-load-balancers.html#connection-idle-timeout) - AWS ALB 연결 유휴 타임아웃 |
| **메트릭스 기반 용량 계획** | SCP 콘솔 예측 메트릭스 확인 또는 CloudWatch forecast | 정상: 용량 예측 모델 적용<br>취약: 미적용 | 트래픽 증가 예측 실패로 용량 부족 발생함, 성능 저하됨 | 메트릭스 기반 예측 스케일링 도입하고, 역사 데이터 분석함 | * 확인방법: Grafana 대시보드 예측 플러그인<br>* 설정방법: Keda 또는 custom metrics 사용<br>* References: [Kubernetes Custom Metrics](https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale-walkthrough/) - Kubernetes 커스텀 메트릭스 HPA 튜토리얼 |

## 운영

| 점검 내용 | 점검 방법 | 점검 기준 | 취약 시 문제점 | 개선 방안 | 참고 |
|-----------|-----------|-----------|---------------|-----------|-------|
| **로그 활성화 및 저장** | SCP 콘솔에서 LB 액세스 로그 설정 확인 또는 `kubectl get svc -o yaml`로 로그 어노테이션 확인 | 정상: 액세스 로그 활성화 및 저장소 설정됨<br>취약: 로그 비활성화 또는 저장 기간 부족 | 로그 미활성화 시 장애 원인 분석 어려움, 보안 감사 불가함, 운영 효율 저하됨 | LB 액세스 로그를 활성화하고, S3나 유사 저장소에 30일 이상 보관하도록 설정함, 로그 포맷을 표준화하여 분석 용이하게 함 | * 확인방법: SCP 콘솔 로그 섹션 또는 `kubectl logs`로 로그 출력 확인<br>* 설정방법: SCP 콘솔에서 로그 활성화 및 저장 버킷 지정<br>* References: [AWS ELB Access Logs](https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-access-logs.html) - AWS ELB 액세스 로그 설정 가이드 |
| **패치 관리 정책** | SCP 콘솔에서 LB 버전 및 패치 로그 확인 또는 `kubectl version` 실행 | 정상: 최신 패치 적용됨<br>취약: 패치 미적용 또는 지연 | 패치 미적용 시 취약점 노출됨, 보안 사고 발생 가능함, 운영 안정성 저하됨 | 정기 패치 일정 수립하고, 롤링 업데이트로 적용함, 패치 후 테스트 수행함 | * 확인방법: `kubectl get nodes -o wide`로 버전 확인<br>* 설정방법: SCP 콘솔에서 자동 패치 활성화<br>* References: [Kubernetes Cluster Upgrade](https://kubernetes.io/docs/tasks/administer-cluster/cluster-upgrade/) - Kubernetes 클러스터 업그레이드 가이드 |
| **백업 및 복구 절차** | SCP 콘솔에서 백업 설정 확인 또는 스냅샷 로그 확인 | 정상: 정기 백업 및 복구 테스트 완료<br>취약: 백업 미설정 또는 테스트 미실시 | 데이터 손실 시 복구 불가함, 운영 중단 기간 연장됨, 비즈니스 손실 발생함 | 일일 백업 스케줄 설정하고, 분기별 복구 테스트 수행함, 백업 저장소를 다중화함 | * 확인방법: SCP 콘솔 백업 섹션 또는 `kubectl get pv`<br>* 설정방법: Velero나 유사 도구로 백업 설정<br>* References: [Velero Kubernetes Backup](https://velero.io/docs/) - Velero Kubernetes 백업 및 복구 설명 |
| **모니터링 도구 통합** | SCP 콘솔에서 모니터링 대시보드 확인 또는 `kubectl get prometheusrules` | 정상: Prometheus나 CloudWatch 통합됨<br>취약: 모니터링 미설정 | 장애 조기 발견 불가함, 운영 대응 지연됨, 성능 문제 누적됨 | Prometheus 설치하고, 메트릭스 수집 및 대시보드 설정함, 알림 규칙 정의함 | * 확인방법: Grafana 대시보드 접속<br>* 설정방법: Helm으로 Prometheus 배포<br>* References: [Prometheus Kubernetes Monitoring](https://prometheus.io/docs/prometheus/latest/federation/) - Prometheus Kubernetes 모니터링 가이드 |
| **접근 제어 설정** | SCP 콘솔에서 IAM 역할 및 정책 확인 | 정상: 최소 권한 원칙 적용됨<br>취약: 과도한 권한 부여 | 무단 접근으로 구성 변경됨, 보안 침해 발생함, 운영 통제 상실됨 | IAM 정책을 role-based로 설정하고, MFA 활성화함, 정기 감사 수행함 | * 확인방법: SCP 콘솔 IAM 섹션<br>* 설정방법: 정책 JSON으로 최소 권한 정의<br>* References: [AWS IAM Best Practices](https://docs.aws.amazon.com/IAM/latest/UserGuide/best-practices.html) - AWS IAM 베스트 프랙티스 가이드 |
| **비용 모니터링** | SCP 콘솔에서 비용 대시보드 확인 또는 청구 로그 분석 | 정상: 비용 예산 설정 및 초과 알림 활성화<br>취약: 비용 모니터링 미설정 | 예상치 못한 비용 증가함, 예산 초과로 운영 예산 압박됨, 리소스 낭비됨 | 비용 태그 적용하고, 예산 알림 설정함, 불필요 리소스 정리함 | * 확인방법: SCP 콘솔 비용 섹션<br>* 설정방법: Budgets 도구로 예산 정의<br>* References: [AWS Cost Management](https://docs.aws.amazon.com/cost-management/latest/userguide/what-is-costmanagement.html) - AWS 비용 관리 가이드 |
| **구성 문서화** | 내부 문서 또는 Git 리포지토리에서 LB 구성 문서 확인 | 정상: 최신 구성 문서 유지됨<br>취약: 문서 미작성 또는 오래됨 | 운영자 간 지식 불균형 발생함, 변경 시 오류 증가함, 온보딩 지연됨 | IaC(Infrastructure as Code) 도구로 구성 관리하고, 문서 자동 업데이트함 | * 확인방법: Terraform 또는 Ansible 스크립트 검토<br>* 설정방법: Git에 구성 YAML 저장<br>* References: [Terraform Kubernetes Provider](https://registry.terraform.io/providers/hashicorp/kubernetes/latest/docs) - Terraform Kubernetes 프로바이더 설명 |
| **장애 시뮬레이션 테스트** | Chaos Engineering 도구로 LB 장애 테스트 수행 | 정상: 정기 테스트 완료 및 보고서 작성<br>취약: 테스트 미실시 | 실제 장애 시 대응 미숙함, 시스템 취약점 미발견됨, 운영 신뢰성 저하됨 | Chaos Monkey나 Gremlin으로 테스트하고, 결과 분석함, 개선 계획 수립함 | * 확인방법: 테스트 로그 확인<br>* 설정방법: Pod Chaos CRD 적용<br>* References: [Chaos Mesh](https://chaos-mesh.org/docs/) - Chaos Mesh 장애 주입 도구 가이드 |
| **업그레이드 관리** | SCP 콘솔에서 LB 버전 히스토리 확인 | 정상: 계획된 업그레이드 절차 준수<br>취약: 무계획 업그레이드 | 업그레이드 실패 시 다운타임 발생함, 호환성 문제 야기됨, 운영 중단됨 | 카나리 배포로 업그레이드하고, 롤백 계획 수립함 | * 확인방법: `kubectl rollout history`<br>* 설정방법: Blue-Green 배포 전략 적용<br>* References: [Kubernetes Rolling Update](https://kubernetes.io/docs/tasks/run-application/rolling-update-deployment/) - Kubernetes 롤링 업데이트 가이드 |
| **EOS 확인** | SCP 문서 또는 벤더 공지에서 LB 컴포넌트 EOS 확인 | 정상: EOS 이전 버전 미사용<br>취약: EOS 버전 사용 | 지원 종료로 패치 불가함, 취약점 노출됨, 운영 위험 증가함 | EOS 이전에 업그레이드 계획 수립하고, 대체 솔루션 검토함 | * 확인방법: SCP 콘솔 버전 정보<br>* 설정방법: 지원 라이프사이클 모니터링<br>* References: [Kubernetes Release Cycle](https://kubernetes.io/docs/reference/releases/) - Kubernetes 릴리스 사이클 설명 |
| **감사 로그 활성화** | SCP 콘솔에서 감사 로그 설정 확인 | 정상: 감사 로그 활성화 및 보관됨<br>취약: 감사 로그 미설정 | 변경 추적 불가함, 보안 사건 조사 어려움, 규정 준수 위반됨 | 감사 로그를 중앙 로그 시스템에 저장하고, 검색 기능 활성화함 | * 확인방법: `kubectl get auditregistration`<br>* 설정방법: Audit Policy YAML 적용<br>* References: [Kubernetes Auditing](https://kubernetes.io/docs/tasks/debug/debug-cluster/audit/) - Kubernetes 감사 로그 가이드 |
| **자동화 스크립트 관리** | Git 리포지토리에서 운영 스크립트 확인 | 정상: 스크립트 버전 관리 및 테스트됨<br>취약: 수동 운영 또는 미관리 스크립트 | 운영 오류 증가함, 반복 작업 지연됨, 일관성 부족함 | Ansible이나 Terraform으로 자동화하고, CI/CD 파이프라인 통합함 | * 확인방법: 스크립트 실행 테스트<br>* 설정방법: GitHub Actions로 자동화<br>* References: [Ansible Kubernetes Modules](https://docs.ansible.com/ansible/latest/collections/kubernetes/core/index.html) - Ansible Kubernetes 모듈 가이드 |
| **리소스 태깅** | SCP 콘솔에서 LB 리소스 태그 확인 | 정상: 환경, 소유자 태그 적용됨<br>취약: 태그 미적용 | 비용 추적 어려움, 리소스 관리 혼란됨, 청구분배 불가함 | 표준 태깅 정책 수립하고, 모든 리소스에 적용함 | * 확인방법: `kubectl get svc --show-labels`<br>* 설정방법: metadata.labels에 태그 추가<br>* References: [AWS Resource Tagging](https://docs.aws.amazon.com/general/latest/gr/aws_tagging.html) - AWS 리소스 태깅 가이드 |
| **네트워크 모니터링** | SCP 콘솔에서 네트워크 메트릭스 확인 | 정상: 네트워크 트래픽 모니터링 활성화<br>취약: 모니터링 미설정 | 네트워크 병목 미발견됨, 성능 문제 지연됨, 운영 대응 느림 | VPC Flow Logs 활성화하고, 분석 도구 통합함 | * 확인방법: SCP 콘솔 네트워크 섹션<br>* 설정방법: Flow Logs 설정<br>* References: [AWS VPC Flow Logs](https://docs.aws.amazon.com/vpc/latest/userguide/flow-logs.html) - AWS VPC 플로우 로그 가이드 |
| **보안 스캔 정기 수행** | SCP 콘솔에서 스캔 보고서 확인 | 정상: 월간 스캔 및 취약점 수정<br>취약: 스캔 미실시 | 취약점 노출됨, 공격 위험 증가함, 운영 보안 약화됨 | Clair나 Trivy로 이미지 스캔하고, Gatekeeper로 정책 적용함 | * 확인방법: 스캔 로그 확인<br>* 설정방법: CI 파이프라인에 스캔 추가<br>* References: [Trivy Vulnerability Scanner](https://aquasecurity.github.io/trivy/latest/) - Trivy 취약점 스캐너 가이드 |
| **인시던트 대응 계획** | 내부 문서에서 IR 계획 확인 | 정상: 계획 문서화 및 훈련 실시<br>취약: 계획 미작성 | 장애 시 혼란 발생함, 복구 지연됨, 피해 확대됨 | IR 플랜 작성하고, 시뮬레이션 훈련 수행함 | * 확인방법: 문서 검토<br>* 설정방법: PagerDuty 통합<br>* References: [NIST Incident Response](https://nvlpubs.nist.gov/nistpubs/SpecialPublications/NIST.SP.800-61r2.pdf) - NIST 인시던트 대응 가이드 |
| **SLA 모니터링** | SCP 콘솔에서 가용성 메트릭스 확인 | 정상: SLA 준수 및 보고<br>취약: SLA 미모니터링 | SLA 위반 시 페널티 발생함, 고객 신뢰 저하됨, 운영 평가 하락됨 | Uptime 메트릭스 추적하고, 초과 시 알림 설정함 | * 확인방법: CloudWatch 대시보드<br>* 설정방법: SLA 대시보드 생성<br>* References: [AWS SLA Monitoring](https://aws.amazon.com/compute/sla/) - AWS 컴퓨트 SLA 설명 |
| **변경 관리 프로세스** | 변경 로그 또는 티켓 시스템 확인 | 정상: 모든 변경 티켓화 및 승인<br>취약: 무계획 변경 | 변경 오류로 장애 발생함, 추적 불가함, 운영 혼란됨 | Change Management 도구 사용하고, PR 프로세스 적용함 | * 확인방법: Jira 또는 GitHub Issues 검토<br>* 설정방법: GitOps 워크플로우<br>* References: [GitOps Principles](https://www.gitops.tech/) - GitOps 원칙 설명 |
| **재해 복구 테스트** | DR 테스트 보고서 확인 | 정상: 연간 DR 테스트 완료<br>취약: 테스트 미실시 | 재해 시 복구 실패함, 비즈니스 연속성 상실됨, 운영 회복 지연됨 | RTO/RPO 정의하고, DR 시뮬레이션 수행함 | * 확인방법: 테스트 로그<br>* 설정방법: Multi-Region 배포<br>* References: [AWS Disaster Recovery](https://aws.amazon.com/disaster-recovery/) - AWS 재해 복구 가이드 |
| **라이선스 관리** | SCP 콘솔에서 라이선스 상태 확인 | 정상: 유효 라이선스 및 갱신 계획<br>취약: 라이선스 만료 | 만료 시 기능 제한됨, 운영 중단됨, 비용 증가함 | 라이선스 인벤토리 관리하고, 자동 갱신 설정함 | * 확인방법: SCP 콘솔 라이선스 섹션<br>* 설정방법: 벤더 포털 모니터링<br>* References: [AWS License Manager](https://docs.aws.amazon.com/license-manager/latest/userguide/license-manager.html) - AWS 라이선스 매니저 가이드 |
| **벤더 지원 티켓 관리** | 지원 티켓 시스템 확인 | 정상: 열린 티켓 없음 또는 처리 중<br>취약: 미처리 티켓 누적 | 문제 해결 지연됨, 운영 이슈 지속됨, SLA 위반됨 | 티켓 우선순위화하고, 에스컬레이션 프로세스 적용함 | * 확인방법: SCP 지원 포털<br>* 설정방법: 정기 리뷰 미팅<br>* References: [Samsung Cloud Support](https://www.samsungsds.com/kr/cloud/support.html) - 삼성SDS 클라우드 지원 페이지 |
| **환경 변수 관리** | `kubectl get configmaps` 또는 secrets 확인 | 정상: ConfigMap/Secret 사용<br>취약: 하드코딩된 변수 | 보안 노출됨, 변경 시 재배포 필요함, 운영 유연성 저하됨 | Secret Manager 통합하고, 동적 로딩 적용함 | * 확인방법: `kubectl describe configmap`<br>* 설정방법: Vault 또는 SSM 사용<br>* References: [Kubernetes Secrets](https://kubernetes.io/docs/concepts/configuration/secret/) - Kubernetes 시크릿 설명 |
| **CI/CD 파이프라인 통합** | CI/CD 도구에서 LB 배포 파이프라인 확인 | 정상: 자동 배포 파이프라인 운영<br>취약: 수동 배포 | 배포 오류 증가함, 릴리스 지연됨, 운영 효율 저하됨 | Jenkins나 ArgoCD로 파이프라인 설정하고, 테스트 스테이지 추가함 | * 확인방법: 파이프라인 로그<br>* 설정방법: YAML 워크플로우 정의<br>* References: [ArgoCD GitOps](https://argoproj.github.io/cd/) - ArgoCD GitOps 도구 가이드 |
| **사용자 액세스 로그** | SCP 콘솔에서 액세스 로그 확인 | 정상: 액세스 로그 기록 및 감사<br>취약: 로그 미기록 | 무단 액세스 미탐지됨, 보안 사고 추적 불가함, 규정 위반됨 | CloudTrail 통합하고, 로그 분석 도구 사용함 | * 확인방법: 로그 쿼리<br>* 설정방법: 액세스 로그 활성화<br>* References: [AWS CloudTrail](https://docs.aws.amazon.com/awscloudtrail/latest/userguide/cloudtrail-user-guide.html) - AWS CloudTrail 사용자 가이드 |
| **운영 대시보드 설정** | Grafana 또는 SCP 대시보드 확인 | 정상: 종합 운영 대시보드 운영<br>취약: 대시보드 미설정 | 운영 상태 한눈에 파악 불가함, 대응 지연됨, 효율 저하됨 | Grafana에 메트릭스, 로그, 알림 통합하고, 공유 대시보드 설정함 | * 확인방법: 대시보드 접속<br>* 설정방법: Prometheus 데이터소스 추가<br>* References: [Grafana Dashboards](https://grafana.com/docs/grafana/latest/dashboards/) - Grafana 대시보드 가이드 |
